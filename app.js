App({
  onLaunch() {
    // TODO: 启动时需要从本地缓存读取用户学习进度
    // 1. 从 storage 中获取已保存的学习记录、词库状态与上次学习时间
    // 2. 校验缓存数据结构与版本，必要时做兼容处理或兜底默认值
    // 3. 将学习进度同步到全局状态，供“学习 / 词库 / 我的”页面初始化使用
    // 4. 如后续接入云端，再在这里补充本地与远端进度合并逻辑
  }
})
